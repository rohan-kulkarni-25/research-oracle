# Research Oracle Configuration

# API Key for authentication (optional - if not set, auth is disabled)
API_KEY=your_api_key_here

# Server port (default: 3000)
PORT=3000

# Anthropic API Key (required for consensus engine)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Solana Configuration (optional - if not set, on-chain attestation is disabled)
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_PRIVATE_KEY=[your,private,key,array,here]
